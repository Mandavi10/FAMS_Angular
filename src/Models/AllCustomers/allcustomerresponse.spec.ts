import { Allcustomerresponse } from './allcustomerresponse';

describe('Allcustomerresponse', () => {
  it('should create an instance', () => {
    expect(new Allcustomerresponse()).toBeTruthy();
  });
});
