import { PortfolioAppraisals } from './portfolio-appraisals';

describe('PortfolioAppraisals', () => {
  it('should create an instance', () => {
    expect(new PortfolioAppraisals()).toBeTruthy();
  });
});
